<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 shadow">
    <div class="container">
        <a class="navbar-brand" href="/users/dashboard">CraftShop</a>
        @if (Context.Session.GetString("username") is not null)
        {
            <p class="navbar-text mb-0">@Context.Session.GetString("username")</p>
        }
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto me-2">
                <li class="nav-item">
                    <a class="nav-link active" href="/crafts/new">Sell a Craft</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/crafts">Shop Crafts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/orders">Order History</a>
                </li>
            </ul>
            @if (Context.Session.GetInt32("userId") is not null)
            {
                <form asp-action="Logout" asp-controller="Users" method="post">
                    <input type="submit" value="LOGOUT" class="btn btn-sm btn-danger">
                </form>
            }
        </div>
    </div>
</nav>